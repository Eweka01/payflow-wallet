apiVersion: v1
kind: ConfigMap
metadata:
  name: payflow-grafana-dashboard
  namespace: monitoring
  labels:
    {{- include "payflow-monitoring.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  payflow-dashboard.json: |
    {{- .Files.Get "files/payflow-dashboard.json" | nindent 4 }}
